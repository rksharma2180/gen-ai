What is the capital of {stateOrCountry} ?

CRITICAL INSTRUCTIONS:
- Respond strictly with a raw JSON object.
- Do NOT include markdown code blocks (```json).
- Do NOT include conversational text, pleasantries, or explanations.

Follow this JSON structure required for parsing:
{format}
"""